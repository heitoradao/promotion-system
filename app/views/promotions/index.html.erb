<% @promotion.each do |promotion| %>
  <h3><%= promotion.name %></h3>
  <dl>
    <dt>Descrição</dt>
    <dd><%= promotion.description %></dd>
    <dt>Desconto <small>em porcentagem</small></dt>
    <dd><%= promotion.discount_rate %></dd>
  </dl>
<% end %>
