<%= form_with(model: categoria_produto) do |form| %>
  <% if categoria_produto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categoria_produto.errors.count, "error") %> prohibited this categoria_produto from being saved:</h2>

      <ul>
        <% categoria_produto.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :promotions_id %>
    <%= form.text_field :promotions_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
